{% extends "base.html.twig" %}
{% block title %} upload your vidoe {% endblock %}
{% block body %}
{{form_start(form)}}
    {{form_row(form.title)}}
    {{form_row(form.description)}}

    {{form_row(form.videoFileName)}}
    <button class="btn btn-dark" type="submit">Upload </button>
{{form_end(form)}}

{% endblock %}